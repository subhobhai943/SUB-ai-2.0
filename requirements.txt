# ================================================================
#  SUB-ai 2.0 — Python Dependencies
# ================================================================
#
#  IMPORTANT: Do NOT run plain `pip install -r requirements.txt`
#  for PyTorch — it will install the CPU-only version.
#
#  Instead, follow these steps in order:
#
#  Step 1 — Install PyTorch with CUDA 12.1 (for RTX 3050 / NVIDIA GPU):
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
#  Step 2 — Install everything else:
#    pip install -r requirements.txt
#
#  Step 3 — Verify GPU is working:
#    python -c "import torch; print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0))"
#
# ================================================================

# ── PyTorch (installed manually via Step 1 above) ──────────────
torch
torchvision
torchaudio

# ── HuggingFace ────────────────────────────────────────────────
# For downloading and using large public datasets
datasets>=2.18.0
huggingface_hub>=0.22.0
tokenizers>=0.19.0
transformers>=4.40.0
peft>=0.10.0
accelerate>=0.29.0

# ── Numerical & Data ───────────────────────────────────────────
numpy>=1.26.0

# ── Training Utilities ─────────────────────────────────────────
tqdm>=4.66.0

# ── GGUF Export / GitHub Release Publishing ────────────────────
# Used by scripts/release_gguf.py to call the GitHub REST API
requests>=2.31.0

# ── Optional: Experiment Tracking ─────────────────────────────
# Uncomment to enable Weights & Biases logging
# wandb

# ── Optional: Jupyter Support ──────────────────────────────────
# Uncomment if you want to experiment in notebooks
# jupyter
# ipykernel
